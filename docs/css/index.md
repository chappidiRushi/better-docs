---
id: css
title: CSS
sidebar_position: 3
---

# **CSS Quick Reference for Hardcore Developers — Final TOC**

---

## 1. CSS Glossary

* Rule, declaration, property, value
* Specified / computed / used / actual values
* Formatting context
* Containing block
* Stacking context
* Replaced elements
* Intrinsic vs extrinsic sizing
* Anonymous boxes
* Initial / inherit / unset / revert / revert-layer
* Origin, layer, importance

---

## 2. CSS Interview Questions & Answers (Senior / Staff Level)

* Explain the cascade step-by-step
* How specificity is calculated (exact rules)
* What creates a stacking context (full list)
* Computed vs used values
* Why `height: 100%` fails
* Flexbox vs Grid (algorithm comparison)
* Margin collapsing rules
* Sticky positioning constraints
* Layout vs paint vs composite triggers
* CSS variables vs preprocessor variables
* What `@layer` actually solves
* Intrinsic sizing explained
* Why `z-index` “doesn’t work”
* Selector matching performance
* Browser error recovery model

---

## 3. CSS Language Fundamentals

* CSS parsing & tokenization
* Rulesets and declarations
* Error handling & invalid CSS
* Forward-compatible parsing
* Shorthand expansion
* Inheritance rules

---

## 4. CSS Values & Resolution Model

* Value lifecycle
* Percentage resolution timing
* Cyclic dependencies
* Dependency graphs
* Initial vs computed values
* How `calc()` participates in resolution

---

## 5. Selectors (Deep Dive)

* Simple & compound selectors
* Attribute selectors
* Pseudo-classes (state, structure, logic)
* Pseudo-elements
* `:is()` / `:where()` / `:has()`
* Specificity math
* Selector matching internals

---

## 6. Cascade, Specificity & Layers

* Cascade origins
* Importance rules
* Specificity edge cases
* Inline styles
* `@layer` ordering
* Layered resets & design systems

---

## 7. Formatting Contexts

* Block formatting context (BFC)
* Inline formatting context
* Flex formatting context
* Grid formatting context
* Table formatting context
* Independent formatting contexts

---

## 8. Box Model & Layout Primitives

* Content-box vs border-box
* Margin collapsing (all rules)
* Padding & borders
* Containing block formation
* Writing modes
* Logical properties

---

## 9. Display, Positioning & Stacking

* Display internal vs external
* Positioning schemes
* Containing blocks by position
* Stacking contexts
* Painting order
* `z-index` resolution

---

## 10. Inline Layout & Text Flow

* Inline formatting algorithm
* Line box construction
* Baseline alignment
* `vertical-align` reality
* Inline replaced elements
* Text wrapping & breaking

---

## 11. Flexbox (Specification-Level)

* Flex formatting context
* Main vs cross axis resolution
* Flex sizing algorithm
* `flex-basis` vs `width`
* Min/max constraints
* Overflow edge cases

---

## 12. Grid Layout (Advanced)

* Explicit vs implicit grids
* Track sizing algorithm
* `fr` unit behavior
* Auto-placement
* Alignment
* Subgrid

---

## 13. Table Layout & Legacy Formatting

* Table box tree
* Auto vs fixed table layout
* Anonymous table boxes
* Intrinsic sizing behavior
* Tables vs grid differences

---

## 14. Sizing & Units

* Absolute & relative units
* Viewport units (dynamic)
* Intrinsic sizing keywords
* Aspect ratio
* Percentage sizing traps

---

## 15. CSS Math & Functions

* `calc()` resolution rules
* `min()` / `max()` / `clamp()`
* Trigonometric functions
* Mixing unit types
* Function fallback behavior

---

## 16. Typography Engine

* Font selection algorithm
* `@font-face` descriptors
* Variable fonts
* Line-height resolution
* Text overflow & wrapping

---

## 17. Colors & Images

* Color spaces (sRGB, P3, OKLCH)
* Color interpolation
* Alpha compositing
* Gradients
* `image-set()`

---

## 18. Backgrounds, Borders & Masks

* Multiple backgrounds
* Border rendering
* `border-radius` math
* Masking
* Clip-path
* Backdrop filters

---

## 19. Replaced Elements

* Images, video, iframes, inputs
* Intrinsic dimensions
* Aspect ratio interactions
* CSS vs UA styling limits
* Object sizing

---

## 20. Transforms, Transitions & Animations

* Transform matrices
* Composite-only properties
* Transition timing rules
* Animation lifecycle
* `will-change` misuse

---

## 21. Scroll, Overflow & Viewport Behavior

* Scroll containers
* Scroll chaining
* `overscroll-behavior`
* Scroll snapping
* Sticky positioning constraints

---

## 22. CSS Variables (Custom Properties)

* Resolution timing
* Fallback chains
* Scope & inheritance
* Performance implications
* Theming architectures

---

## 23. Media Queries & Container Queries

* Media evaluation model
* Range syntax
* Interaction queries
* Container size queries
* Style queries

---

## 24. Accessibility-Relevant CSS

* Visual vs semantic hiding
* Focus management
* Forced colors mode
* Reduced motion
* Contrast pitfalls

---

## 25. User Interaction & Input Styling

* Pointer & hover media features
* `touch-action`
* `user-select`
* Hit-testing
* Caret styling

---

## 26. Performance & Rendering Pipeline

* Style calculation
* Layout thrashing
* Paint vs composite
* CSS containment
* `content-visibility`
* GPU myths

---

## 27. Scoping, Shadow DOM & Encapsulation

* Shadow tree styling rules
* `:host`
* `::part`
* CSS variable piercing
* Style isolation

---

## 28. Interop, Security & Spec Gaps

* Vendor prefixes
* Undefined behavior zones
* Visited link privacy
* Font fingerprinting limits
* Spec vs implementation gaps

---

## 29. Tooling & Debugging

* DevTools deep inspection
* Computed vs used values
* Layout debugging
* Source maps
* Visual regression testing

---

## 30. Architecture & Scaling CSS

* BEM / ITCSS / OOCSS (reality check)
* Utility-first systems
* Design tokens
* CSS-in-JS tradeoffs
* Layer-first architectures

---

## 31. Modern & Emerging CSS

* Nesting
* Scoped styles
* Anchor positioning
* Scroll-driven animations
* Shipping readiness

---

## 32. Paged Media & Print CSS

* `@page`
* Page margin boxes
* Page breaks
* Print cascade differences

---

## 33. CSS Gotchas & Failure Modes

* Percentage height traps
* Auto margin behavior
* Flex overflow bugs
* Grid min-content surprises
* Stacking context nightmares

---
