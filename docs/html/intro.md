



This book covers following;

## 1. HTML as a Platform

1. What HTML Is (and Is Not)
2. HTML vs XML vs XHTML
3. HTML Living Standard (WHATWG) vs W3C
4. The Web Platform Stack (HTML, CSS, JS, DOM, Web APIs)
5. Conformance, Validators, and Compliance Levels

---

## 2. HTML Document Model

1. The HTML Document Lifecycle
2. Document Types (`<!DOCTYPE>`) and Standards Mode
3. Quirks Mode and Almost Standards Mode
4. Document Tree Construction
5. The DOM vs Source Markup
6. Node Types and Tree Relationships

---

## 3. Parsing Algorithms (Critical for Experts)

1. Tokenization Algorithm
2. Tree Construction Algorithm
3. Error Handling and Error Recovery
4. Foster Parenting
5. Implied Tags and Omitted End Tags
6. Foreign Content (SVG & MathML Integration)
7. Script Execution During Parsing
8. Blocking vs Non-Blocking Parsing

---

## 4. Global HTML Concepts

1. Elements, Tags, and Attributes
2. Void Elements
3. Boolean Attributes
4. Global Attributes
5. Enumerated Attributes
6. Data Attributes (`data-*`)
7. ARIA Attributes (HTML vs ARIA Conflicts)
8. Content Categories
9. Transparent Content Model

---

## 5. Metadata and Document Head

1. `<html>` Element
2. `<head>` Element
3. `<meta>` (charset, viewport, SEO, security)
4. `<title>` and Accessibility
5. `<base>`
6. `<link>` (preload, prefetch, DNS, icons)
7. `<style>`
8. `<script>` in Head
9. Resource Hints and Performance

---

## 6. Sectioning and Document Structure

1. Sectioning Content Model
2. `<main>`
3. `<section>`
4. `<article>`
5. `<aside>`
6. `<nav>`
7. `<header>` and `<footer>`
8. Heading Algorithm (and Why It’s Controversial)
9. Landmark Roles

---

## 7. Text-Level Semantics

1. Inline vs Block Semantics
2. `<p>` Parsing Rules
3. `<span>` vs Semantic Alternatives
4. `<strong>` vs `<b>`
5. `<em>` vs `<i>`
6. `<small>`, `<mark>`, `<abbr>`
7. `<cite>`, `<q>`, `<blockquote>`
8. `<code>`, `<pre>`, `<kbd>`, `<samp>`
9. `<time>`
10. `<ruby>` and East Asian Typography

---

## 8. Lists and Grouping Content

1. `<ul>`, `<ol>`, `<li>`
2. Definition Lists (`<dl>`, `<dt>`, `<dd>`)
3. `<figure>` and `<figcaption>`
4. `<hr>`
5. `<div>` as a Last Resort

---

## 9. Links and Navigation

1. `<a>` Element Deep Dive
2. URL Resolution Algorithm
3. Fragment Identifiers
4. Link Types (`rel` Values)
5. Security Implications (`noopener`, `noreferrer`)
6. Download Attribute
7. Accessibility and Focus Behavior

---

## 10. Media Elements

1. `<img>` Internals
2. Responsive Images (`srcset`, `sizes`)
3. `<picture>`
4. `<audio>`
5. `<video>`
6. `<track>` and Text Tracks
7. Media Loading and Decoding
8. CORS and Media
9. Media Error States

---

## 11. Embedded and External Content

1. `<iframe>` Internals
2. Browsing Contexts
3. Sandboxing
4. `srcdoc`
5. `<embed>`
6. `<object>`
7. Legacy Embedding Pitfalls

---

## 12. Tables (Full Spec Coverage)

1. Table Formatting Model
2. `<table>`, `<caption>`
3. `<colgroup>`, `<col>`
4. `<thead>`, `<tbody>`, `<tfoot>`
5. `<tr>`, `<th>`, `<td>`
6. Header Associations
7. Accessibility Mapping
8. Styling vs Semantics

---

## 13. Forms Architecture

1. Form Submission Model
2. `<form>` Element
3. Controls Overview
4. `<input>` Types (All of Them)
5. `<textarea>`
6. `<select>`, `<option>`, `<optgroup>`
7. `<button>`
8. `<label>`
9. `<fieldset>` and `<legend>`
10. `<datalist>`
11. `<output>`
12. Constraint Validation API
13. Native Validation UX
14. Autofill and Password Managers

---

## 14. Interactive Elements

1. `<details>` and `<summary>`
2. `<dialog>`
3. Popover API
4. Focus Management
5. Keyboard Interaction Models

---

## 15. Scripting and HTML

1. `<script>` Element Deep Dive
2. Script Types (`module`, `importmap`)
3. Async vs Defer vs Blocking
4. Inline vs External Scripts
5. Execution Order Guarantees
6. Interaction with DOM Parsing
7. Security Restrictions

---

## 16. Template and Reuse Mechanisms

1. `<template>`
2. Document Fragments
3. Cloning and Instantiation
4. Shadow DOM Interaction

---

## 17. HTML and Accessibility (A11y)

1. Native Semantics vs ARIA
2. Accessibility Tree Mapping
3. Landmark Roles
4. Focus Order and Tab Navigation
5. Keyboard Accessibility
6. Screen Reader Behavior
7. Common Anti-Patterns

---

## 18. HTML and Security

1. Same-Origin Policy
2. CORS Basics
3. Content Security Policy (CSP)
4. XSS and HTML Injection
5. Clickjacking
6. Sandbox Mechanisms
7. Trusted Types

---

## 19. Internationalization (i18n)

1. Language Declaration (`lang`)
2. Directionality (`dir`)
3. Bidi Algorithm Basics
4. Character Encodings
5. Unicode Normalization
6. Localization Pitfalls

---

## 20. Performance Considerations

1. Critical Rendering Path
2. HTML Size and Compression
3. Streaming HTML
4. Speculative Parsing
5. Resource Prioritization
6. Lazy Loading
7. HTML and Core Web Vitals

---

## 21. HTML and Web Components

1. Custom Elements Overview
2. HTML Extensions via Custom Elements
3. Slotting and Composition
4. Declarative Shadow DOM
5. Limitations of HTML Alone

---

## 22. HTML Specification Deep Reading Guide

1. How to Read the HTML Spec
2. Keywords and Normative Language
3. Algorithms vs Requirements
4. Tracking Spec Changes
5. Participating in Standards

---
